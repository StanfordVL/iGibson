version: "3.7"

services:
  igibson:
    image: docker.io/igibson/igibson:${VERSION}
    build:
      context: ./igibson
      dockerfile: Dockerfile

  igibson-tensorflow:
    image: docker.io/igibson/igibson:${VERSION}-tensorflow
    build:
      context: ./tensorflow
      dockerfile: Dockerfile
